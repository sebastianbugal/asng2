<!DOCTYPE html>
<html>
<head>
  <style>
    input{
    width: 100%;
    padding: 12px 20px;
    margin: 8px 0;
    box-sizing: border-box;
  }
  
  select{
    width: 100%;
    padding: 12px 20px;
    margin: 8px 0;
    box-sizing: border-box;
  }
  .button {
      background-color: white;
      color: black;
      border: 2px solid #252525; /* Green */
      width:100%;
      height:auto;
      padding:5px;
      display: inline-block;
      margin:2px;
      font-size: large;
      transition-duration: 0.4s;
    }
    .p1{
      display: inline-block;
    }
    .button:hover{
      background-color: rgb(141, 141, 141);
      color: black;
      border: 2px solid #252525; /* Green */
      width:100%;
      height:auto;
      padding:5px;
      /* display: inline-block; */
      font-size: large;
      transition-duration: 0.4s;
    }
    .select{
      display: inline-block;
      margin:2px;
      padding:2px;
      width:auto;
      height:auto;
      border:1px solid #000;
      background-color: rgb(101, 117, 112);
    }
    .norm{
      display: inline-block;
      margin:2px;
      padding:2px;
      width:auto;
      height:auto;
      border:1px solid #000;
      background-color: aquamarine;
      cursor: pointer;
    }
  </style>
  <% include ../partials/header.ejs %>
</head>

<body>


        <% include ../partials/nav.ejs %>
</div>
<div class="container">
  <h1>User ID: <%=user[0].uid%></h1>
  <form class="" action="/send_update" method="post">
    <h3>Name:</h3>
      <input type="text" name="name_upload" value=<%=user[0].name%> placeholder="Name" required><br>
      <h3>Age:</h3>
      <input type="number" name="age_upload" value=<%=user[0].age%> placeholder="Age" required><br>
      <h3>Height:</h3>
      <input type="number" name="height_upload" value=<%=user[0].height%> placeholder="Height" required><br>
      <h3>Sex:</h3>
      <select id="sex" name="sex_upload">
          <% if(user[0].sex == 'Male'){ %>
        <option value="Male" selected>Male</option>
        <option value="Female">Female</option>
        <% }  else{ %>
            <option value="Male" >Male</option>
            <option value="Female" selected>Female</option>
            <% } %>

      </select>     
      <h3>Chess Elo:</h3>
      <input type="number" name="chess_elo_upload" value=<%=user[0].chess_elo%>   placeholder="Chess Elo" required><br>
      <h3>Type:</h3>
      <input style="text-transform: capitalize;" type="text" name="type" value=<%=user[0].type%>   placeholder="type" required><br>

      <h3 class="text-center">Click Submit to update user <%=user[0].uid%></h3>
      <input type="submit" name="sub" value="Submit" class="button" >
      <input id="user_selected_update" name="user_selected_update" type="hidden" value= <%=user[0].uid%> ><br>

  </form>
  <form class="" action="/display" method="get">
    <input class="button"  type="submit" name="sub" value="Back">
  </form>

</div>
</body>
</html>
